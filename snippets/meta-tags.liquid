{%- liquid
  assign seo_title = page_title | default: shop.name
  assign seo_description = page_description | default: shop.description | default: shop.name
  assign seo_image = page_image
  if seo_image == blank and product
    assign seo_image = product.featured_image
  endif
  if seo_image == blank and collection
    assign seo_image = collection.image
  endif
-%}
<title>{{ seo_title }}{% unless seo_title contains shop.name %} | {{ shop.name }}{% endunless %}</title>
{%- if seo_description != blank -%}
  <meta name="description" content="{{ seo_description | strip_html | truncate: 320 }}">
{%- endif -%}
<link rel="canonical" href="{{ canonical_url }}">

<meta property="og:site_name" content="{{ shop.name }}">
<meta property="og:url" content="{{ canonical_url }}">
<meta property="og:title" content="{{ seo_title }}">
<meta property="og:type" content="{% if product %}product{% else %}website{% endif %}">
{%- if seo_description != blank -%}
  <meta property="og:description" content="{{ seo_description | strip_html | truncate: 320 }}">
{%- endif -%}
{%- if seo_image != blank -%}
  <meta property="og:image" content="https:{{ seo_image | image_url: width: 1200 }}">
  <meta property="og:image:width" content="1200">
  <meta name="twitter:card" content="summary_large_image">
{%- else -%}
  <meta name="twitter:card" content="summary">
{%- endif -%}
<meta name="twitter:title" content="{{ seo_title }}">
{%- if seo_description != blank -%}
  <meta name="twitter:description" content="{{ seo_description | strip_html | truncate: 320 }}">
{%- endif -%}

{%- render 'structured-data-organization' -%}
{%- render 'structured-data-breadcrumb' -%}
